spring.application.name=btvn5
# ??i port m?c ??nh
server.port=8080

# S? thread t?i ?a
server.tomcat.threads.max=200

# S? thread t?i thi?u (luôn s?n sàng)
server.tomcat.threads.min-spare=10

# Timeout cho request (m?c ??nh 60s)
server.tomcat.connection-timeout=20000

# Context path
server.servlet.context-path=/api/v1
# === Database Connection ===
spring.datasource.url=jdbc:mysql://localhost:3306/hit_springboot?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=05022005
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# === JPA / Hibernate ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
